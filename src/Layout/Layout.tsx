import React from 'react';
import Navigation from '../Components/Navigation/Navigation';

const Layout = () => {
    return (
        <React.Fragment>
            <Navigation Menu={[]} Status={'dark'}></Navigation>
        </React.Fragment>
    );
};

export default Layout;
